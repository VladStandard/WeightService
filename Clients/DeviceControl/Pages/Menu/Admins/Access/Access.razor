@page "/section/access"
@attribute [Authorize(Roles = WsUserAccessStr.Read)]
@using WsDataCore.Utils
@using DeviceControl.Components.Section
@using DeviceControl.Components.Rz
@using DeviceControl.Utils
@using WsStorageCore.Tables.TableScaleModels.Access
@inherits SectionBase<WsStorageCore.Tables.TableScaleModels.Access.WsSqlAccessModel>

<SectionTopBar
  Title="@WsLocaleCore.Strings.AccessRights"
  OnSectionUpdate="@GetSectionData"
  OnSectionAdd="@SqlItemNewAsync"
  SectionCount="@SqlSectionCast.Count"
  SqlCrudConfigSection="@SqlCrudConfigSection"
  IsGuiShowFilterMarked="@IsGuiShowFilterMarked"
  ButtonSettings="@ButtonSettings"/>

<RzDataGrid
  TItem="@WsSqlAccessModel"
  @ref="@DataGrid"
  @bind-Value="@SelectedRow"
  Data="@SqlSectionCast"
  CellContextMenu="@OnCellContextMenu"
  RowSelect="@SqlItemSet"
  RowDoubleClick="@SqlItemOpenAsync">
  <Columns>
    <TItemContent TItem="@WsSqlAccessModel">
      <SectionIsMarked SqlCrudConfigSection="@SqlCrudConfigSection"/>
      <RadzenDataGridColumn TItem="@WsSqlAccessModel" Title="@WsLocaleCore.Table.AccessLevel"
                            SortProperty="Rights" Width="30%">
        <Template Context="itemCast">
          @GetAccessRightsDescription(itemCast.Rights)
        </Template>
      </RadzenDataGridColumn>
      <RadzenDataGridColumn Title="@WsLocaleCore.Table.User" Property="Name" Width="30%" />
      <RadzenDataGridColumn Title="@WsLocaleCore.Table.LoginDt" Property="LoginDt" Width="30%"
                            FormatString="@WsStrUtils.FormatDtLong"/>
    </TItemContent>
  </Columns>
</RzDataGrid>
