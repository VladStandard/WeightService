@attribute [Authorize(Roles = WsUserAccessStr.Read)]
@using DeviceControl.Components.Section
@using DeviceControl.Components.Rz
@using DeviceControl.Utils
@using WsStorageCore.Tables.TableScaleModels.PlusScales
@inherits SectionBase<WsStorageCore.Tables.TableScaleModels.PlusScales.WsSqlPluScaleModel>

<SectionTopBar
  Title="@WsLocaleCore.DeviceControl.SectionPlusScales"
  OnSectionUpdate="@GetSectionData"
  OnSectionAdd="@SqlItemNewAsync"
  SectionCount="@SqlSectionCast.Count"
  SqlCrudConfigSection="@SqlCrudConfigSection"
  IsGuiShowFilterMarked="false"
  ButtonSettings="@ButtonSettings"/>

<RzDataGrid
  TItem="@WsSqlPluScaleModel"
  @ref="@DataGrid"
  @bind-Value="@SelectedRow"
  Data="@SqlSectionCast"
  IsLoading="@IsLoading"
  CellContextMenu="@OnCellContextMenu"
  RowSelect="@SqlItemSet"
  RowDoubleClick="@SqlItemOpenAsync">
  <Columns>
    <TItemContent TItem="@WsSqlPluScaleModel">
      <SectionIsMarked SqlCrudConfigSection="@SqlCrudConfigSection"/>
      <RadzenDataGridColumn Title="@WsLocaleCore.Table.Plu" Property="Plu.Name" />
      <RadzenDataGridColumn Title="@WsLocaleCore.Table.NumberShort" Property="Plu.Number" />
      <RadzenDataGridColumn Title="@WsLocaleCore.Table.WeightShort" SortProperty="Plu.IsCheckWeight">
        <Template Context="itemCast">
          <RadzenCheckBox ReadOnly="true" TValue="bool" @bind-Value="@itemCast.Plu.IsCheckWeight"/>
        </Template>
      </RadzenDataGridColumn>
    </TItemContent>
  </Columns>
</RzDataGrid>

