namespace DeviceControlCore.DAL.DataModels
{
    public enum EnumDb
    {
        Debug,
        Release,
    }

    public enum EnumField
    {
        Uid,
        Id,
        Name,
        Value,
        Description,
        ScaleId,
        CategoryId,
        Title,
        CreateDate,
        ModifiedDate,
        Type,
        Plu,
        Marked,
        GoodsName,
        WeithingDate
    }

    public enum EnumOrderDirection
    {
        Asc,
        Desc
    }
}
