@inherits MdmControlBlazor.Data.BaseRazorEntity
@page "/login"
@using BlazorCore
@using DataCore
@using LocalizationCore = DataCore.Localization.Core

<AuthorizeView>
    <Authorizing>
        <div>@LocalizationCore.Strings.Main.AuthorizingProcess</div>
    </Authorizing>
    <Authorized>
        @if (context.User.Identity != null)
        {
            //BlazorSettings.State = context;
            <div>@context.User.Identity.Name</div>
        }!
        <div>User detect error!</div>
    </Authorized>
    <NotAuthorized>
        <div>@LocalizationCore.Strings.Main.AuthorizingNot</div>
    </NotAuthorized>
</AuthorizeView>
