@using DataCore.Sql.TableScaleModels.Bundles
@using BlazorDeviceControl.Pages.CustomComponents
@inherits RazorComponentSectionBase<BundleModel>
@page "/section/bundles"
@attribute [Authorize(Roles = UserAccessStr.Read)]

<RazorSectionReload
  TItem="@BundleModel"
  Title="@LocaleCore.DeviceControl.SectionBundles"
  OnSectionUpdate="@GetSectionData"
  SectionCount="@SqlSectionCast.Count"
  SqlCrudConfigSection="@SqlCrudConfigSection"
  ButtonSettings="@ButtonSettings"
  />

<RadzenDataGridLocal
  @bind-Value="@SelectedRow"
  TItem="@BundleModel"
  Data="@SqlSectionCast"
  CellContextMenu="@OnCellContextMenu"
  RowSelect="@SqlItemSet"
  RowRender="@RowRender"
  RowDoubleClick="@SqlItemEditAsync">
  <Columns>
    <TItemContent TItem="@BundleModel">
      <RazorSectionFieldIsMarked SqlCrudConfigSection="@SqlCrudConfigSection" />
      <RadzenDataGridColumn Title="@LocaleCore.Table.Name" Property="Name" Width="75%" />
      <RadzenDataGridColumn Title="@LocaleCore.Table.BundleWeight" Property="Weight" />
    </TItemContent>
  </Columns>
</RadzenDataGridLocal>
