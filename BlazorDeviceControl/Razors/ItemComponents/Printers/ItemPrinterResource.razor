@using DataCore.Sql.TableScaleModels.TemplatesResources
@using DataCore.Sql.TableScaleModels.Printers
@using BlazorDeviceControl.Razors.CustomComponents
@using DataCore.Sql.TableScaleFkModels.PrintersResourcesFks
@inherits RazorComponentItemBase<DataCore.Sql.TableScaleFkModels.PrintersResourcesFks.PrinterResourceFkModel>
@page "/item/printer_resource"
@page "/item/printer_resource/new"
@page "/item/printer_resource/{IdentityId:long}"

<RazorItemReload TItem="@PrinterResourceFkModel" ParentRazor="@(this)" Title="@LocaleCore.Print.Resource"/>

<MudSimpleTable Class="MudItemTable" Dense="true" Bordered="true" FixedHeader="false">
  <RazorItemTableHead CssTableStyleHead="@RazorComponentUtils.GetTableHeadStyle(new() { 15, 35, 15, 35 })"/>
  <tbody>
  <RazorItemTableBody TItem="@PrinterResourceFkModel" ParentRazor="@(this)"/>
  <tr>
    <td>@LocaleCore.Print.Name</td>
    <td>
      <MudSelectLocal @bind-Value="@SqlItemCast.Printer">
        @foreach (PrinterModel item in DataContext.Printers)
        {
          <MudSelectItem Value=@item>@item.Name</MudSelectItem>
        }
      </MudSelectLocal>
    </td>
    <td>@LocaleCore.Table.Resource</td>
    <td>
      <MudSelectLocal @bind-Value="@SqlItemCast.TemplateResource">
        @foreach (TemplateResourceDeprecatedModel item in DataContext.TemplateResources)
        {
          <MudSelectItem Value=@item>@item.Name</MudSelectItem>
        }
      </MudSelectLocal>
    </td>
  </tr>
  <tr>
    <td>@LocaleCore.Table.Description</td>
    <td colspan="3">
      <RadzenTextBox Style="width: 100%; " @bind-Value="@SqlItemCast.Description"/>
    </td>
  </tr>
  </tbody>
</MudSimpleTable>

<RazorItemDates SqlItem="@SqlItem"/>
<RazorItemActions TItem="@PrinterResourceFkModel" ButtonSettings="@ButtonSettings" ParentRazor="@(this)"/>
