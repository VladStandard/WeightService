@typeparam TItem
@inherits RazorComponentItemBase<TItem>
@page "/item/save"
@using BlazorCore.Razors
@using BlazorCore.Utils
@using DataCore.Localizations
@using MudBlazor
@using Radzen
@using Radzen.Blazor
@using BlazorDeviceControl.Razors.SystemComponents

<SystemIdentity ParentRazor=@(this) />

<MudSimpleTable Style="margin-top: -10px;" Dense=true Bordered=false FixedHeader=false>
	<thead>
		<tr>
			<th style="color: blue; font-weight: normal; width: 50%;"></th>
			<th style="color: blue; font-weight: normal; width: 50%;"></th>
		</tr>
	</thead>
	<tbody>
		<tr>
			@if (SqlItem is not null)
			{
				<td>
					@if (ButtonSettings is not null && ButtonSettings.IsShowSave && UserSettings is not null && UserSettings.AccessRightsIsWrite)
					{
						<RadzenButton 
							class=@BlazorCoreUtuls.GetCssName.RadzenButtonLong 
							Click=@(_ => SqlItemSaveAsync())
							ButtonStyle=@ButtonStyle.Primary Size=@ButtonSize.Small
							Style="width: 100%; "
							Text=@LocaleCore.DeviceControl.TableActionSave 
						/>
					}
				</td>
				<td>
					@if (ButtonSettings is not null && ButtonSettings.IsShowCancel && UserSettings is not null && UserSettings.AccessRightsIsRead)
					{
						<RadzenButton 
							class=@BlazorCoreUtuls.GetCssName.RadzenButtonLong 
							Click=@(_ => SqlItemCancelAsync())
							ButtonStyle=@ButtonStyle.Secondary Size=@ButtonSize.Small
							Style="width: 100%; "
							Text=@LocaleCore.DeviceControl.TableActionCancel 
						/>
					}
				</td>
			}
		</tr>
	</tbody>
</MudSimpleTable>
