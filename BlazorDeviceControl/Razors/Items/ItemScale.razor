@inherits BlazorCore.Models.RazorBase
@page "/item/scale"
@page "/item/scale/{Identity.Id:long}"
@page "/item/scale/{Identity.Id:long}/{TableAction}"

<ItemScaleCore Identity.Id=Identity.Id TableAction=TableAction IsPluNew=true />
