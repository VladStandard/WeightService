@inherits BlazorCore.Models.RazorPageItemBase<ProductionFacilityModel>
@page "/item/productionfacility"
@page "/item/productionfacility/{IdentityId:long}"
@page "/item/productionfacility/{IdentityId:long}/{TableAction}"
@using BlazorDeviceControl.Razors.Components
@using BlazorCore.Razors
@using BlazorDeviceControl.Razors.Items.Componets

@if (!IsActionsParametersSetFinished)
{
    <ActionsLoad ParentRazor=@(this) DataLoadItem=ActionLoadEnum.Loading IsShowProgress=true />
}
else
{
	<ItemReload T=ProductionFacilityModel ParentRazor=@(this) Title=@LocaleCore.DeviceControl.ItemProductionFacility />

	<MudSimpleTable Style="overflow-x: auto;" Dense=true Bordered=true FixedHeader=false>
		<ItemTableHead ParentRazor=@(this) TableStyle=@ComponentsUtils.GetTableHeadStyle(new() { 20, 30, 20, 30 }) />
		<tbody>
			<ItemTableBody ParentRazor=@(this) TableStyle=@ComponentsUtils.GetTableBodyStyle(SqlFieldIdentityEnum.Id, true) />
			<tr>
				<td>@LocaleCore.Table.Name</td>
				<td colspan=3>
					<RadzenTextBox Style="width: 100%; text-align: left;"
					               Disabled=false ReadOnly=false
					               TValue="string" @bind-Value=@ItemCast.Name/>
				</td>
			</tr>
			<tr>
				<td >@LocaleCore.Table.Address</td>
				<td colspan=3>
					<RadzenTextArea Style="width: 100%; text-align: left;"
					                Disabled=false ReadOnly=false Rows=3
					                TValue="string" @bind-Value=@ItemCast.Address/>
				</td>
			</tr>
		</tbody>
	</MudSimpleTable>

	<ItemSave T=ProductionFacilityModel ParentRazor=@(this) />
	<ItemDates T=ProductionFacilityModel ParentRazor=@(this) />
}
