@inherits BlazorCore.Models.RazorDisposable
@page "/item/taskmodule"
@page "/item/taskmodule/{UidStr}"
@page "/item/taskmodule/{UidStr}/{TableActionString}"
@using BlazorCore
@using BlazorDeviceControl.Shared.Component
@using BlazorDeviceControl.Shared.Section
@using DataCore
@using DataCore.Utils

@if (ItemCast == null)
{
    <ActionsLoad ParentRazor=@this DataLoadItem=ShareEnums.ActionLoad.Loading IsShowProgress=true />
}
else
{
    <ActionsReload ParentRazor=@this Title=@LocalizationData.DeviceControl.Items.TaskModule SetParameters=@SetParametersAsync 
        IsShowItemsCount=false IsShowMarkedFilter=false />

    <TaskModules Uid=@ItemCast?.IdentityUid />
    <ActionsSave ParentRazor=@this ButtonSettings=@ButtonSettings />
}
