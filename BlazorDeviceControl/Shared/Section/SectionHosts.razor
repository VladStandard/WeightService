@inherits BlazorCore.Models.RazorBase
@page "/section/hosts"
@using BlazorCore
@using BlazorDeviceControl.Shared.Component
@using BlazorDeviceControl.Shared.Item
@using DataCore
@using DataCore.Sql
@using DataCore.Sql.DataModels
@using DataCore.Sql.TableScaleModels
@using DataCore.Localizations
@using DataCore.Utils
@using static DataCore.ShareEnums

@if (!IsLoaded)
{
    <ActionsLoad DataLoadItem=ShareEnums.ActionLoad.Loading IsShowProgress=true />
}
else
{
    <ActionsReloadSection ParentRazor=@this 
        Title=@LocaleData.DeviceControl.Sections.Hosts SetParameters=@SetParametersAsync />

    <RadzenGrid Style="margin-top: 5px;"
        Data=@ItemsCast TItem="HostEntity" @bind-Value=@ItemObject
        RowDoubleClick=@(args => ActionEditAsync(UserSettings, false, false))
        AllowFiltering=true FilterCaseSensitivity=FilterCaseSensitivity.CaseInsensitive FilterMode="FilterMode.Advanced"
        AllowPaging="true" PageSize=@AppSettings.DataAccess.JsonSettingsLocal.SectionRowsCount AllowSorting=true ColumnWidth="100px">
        <Columns>
            @if (@IsShowMarkedItems)
            {
                <RadzenGridColumn TItem="HostEntity" TextAlign=TextAlign.Center 
                    Title=@LocaleCore.Table.MarkedShort Type="bool" Width="3.5%">
                    <Template Context="itemCast">
                        <RadzenIcon Icon=@(itemCast.IsMarked ? "delete" : "") />
                    </Template>
                </RadzenGridColumn>
            }
            <RadzenGridColumn 
                TItem="HostEntity" Title=@LocaleCore.Table.Name Width="25.0%" >
                <Template Context="itemCast">
                    <RadzenLink Style="width: 100%; center; margin-left: 0px; "
                        Path=@($"{LocaleData.DeviceControl.UriRouteItem.Host}/{itemCast.IdentityId}")
                        Text=@itemCast.Name />
                </Template>
            </RadzenGridColumn>
            <RadzenGridColumn 
                TItem="HostEntity" Property="Ip" Title=@LocaleCore.Table.DeviceIp
                Type="string" Width="25.0%" />
            <RadzenGridColumn 
                TItem="HostEntity" Property="IdRRef" Title=@LocaleCore.Table.IdRRef
                Type="string" Width="25.0%" />
            <RadzenGridColumn 
                TItem="HostEntity" TextAlign=TextAlign.Center Property="MacAddress.ValuePrettyLookMinus" Title=@LocaleCore.Table.DeviceMac
                Type="string" Width="25.0%" />
        </Columns>
    </RadzenGrid>

    <ActionsButtons ParentRazor=@this ButtonSettings=@ButtonSettings />
}
