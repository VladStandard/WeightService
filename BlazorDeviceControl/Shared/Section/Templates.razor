@page "/section/templates"
@using BlazorCore.Models
@using DataProjectsCore
@using DataProjectsCore.DAL
@using DataProjectsCore.DAL.DataModels
@using DataProjectsCore.DAL.TableModels
@using DataProjectsCore.DAL.TableScaleModels
@using DataProjectsCore.Utils
@using BlazorDeviceControl.Shared.Component
@using BlazorDeviceControl.Shared.Item
@using DataShareCore
@inherits RazorDisposable

<DataReload Title=@LocalizationData.DeviceControl.SectionTemplates SetParameters=@SetParametersAsync IsShowItemsCount=true Items=@Items />

<RadzenGrid Style="margin-top: 5px;"
    Data=@ItemsCast TItem="TemplateEntity"
    RowSelect=@(args => ItemSelectAsync(args)) 
    RowDoubleClick=@(args => ActionAsync(UserSettings, ShareEnums.DbTableAction.Edit, false))
    AllowFiltering="true" FilterCaseSensitivity="FilterCaseSensitivity.CaseInsensitive" FilterMode="FilterMode.Advanced"
    AllowPaging="true" PageSize=20 AllowSorting="true" ColumnWidth="100px">
    <Columns>
        <RadzenGridColumn 
            TItem="TemplateEntity" Property="Id" TextAlign="TextAlign.Center"
            Title=@LocalizationData.DeviceControl.TableFieldId Width="6%" />
        <RadzenGridColumn 
            TItem="TemplateEntity" Property="CategoryId" TextAlign="TextAlign.Center"
            Title=@LocalizationData.DeviceControl.TableFieldCategoryName Width="10%" />
        <RadzenGridColumn 
            TItem="TemplateEntity" Property="Title" 
            Title=@LocalizationData.DeviceControl.TableFieldTitle Width="84%" />
    </Columns>
</RadzenGrid>
@if (Items == null)
{
    <DataLoad DataLoadItem=ShareEnums.DataLoad.Loading IsShowProgress=true />
}
else
{
    <SectionActions Table=@Table Id=@Id IsShowNew=true IsShowEdit=true IsShowCopy=true IsShowMark=true IsShowDelete=true />
}
