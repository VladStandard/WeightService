@inherits BlazorCore.Models.RazorDisposable
@page "/section/templateresources"
@using BlazorCore
@using BlazorDeviceControl.Shared.Component
@using BlazorDeviceControl.Shared.Item
@using DataCore
@using DataCore.DAL
@using DataCore.DAL.DataModels
@using DataCore.DAL.TableScaleModels
@using DataCore.Utils
@using static DataCore.ShareEnums

@if (ItemsCast == null)
{
    <ActionsLoad DataLoadItem=ShareEnums.ActionLoad.Loading IsShowProgress=true />
}
else
{
    <ActionsReload ParentRazor=@this Items=@Items 
        Title=@LocalizationData.DeviceControl.SectionTemplateResources SetParameters=@SetParametersAsync IsShowItemsCount=true />

    <RadzenGrid Style="margin-top: 5px;" 
        Data=@ItemsCast TItem="TemplateResourceEntity" @bind-Value=@ItemObject
        RowDoubleClick=@(args => ActionAsync(UserSettings, DbTableAction.Edit, false, false))
        AllowFiltering=true FilterCaseSensitivity=FilterCaseSensitivity.CaseInsensitive FilterMode="FilterMode.Advanced"
        AllowPaging="true" PageSize=@AppSettings.DataAccess.JsonSettings.SectionRowCount AllowSorting=true ColumnWidth="100px">
        <Columns >
            <RadzenGridColumn 
                TItem="TemplateResourceEntity" Property="Type" TextAlign=TextAlign.Center
                Title=@LocalizationData.DeviceControl.TableFieldType 
                Type="string" Width="10%" />
            <RadzenGridColumn 
                TItem="TemplateResourceEntity" Title=@LocalizationData.DeviceControl.TableFieldName Width="25%" >
                <Template Context="itemCast">
                    <RadzenLink Style="width: 100%; margin-left: 0px; "
                        Path=@($"{@LocalizationData.DeviceControl.UriRouteItem.TemplateResource}/{itemCast.Id}")
                        Text=@itemCast.Name />
                </Template>
            </RadzenGridColumn>
            <RadzenGridColumn 
                TItem="TemplateResourceEntity" Property="Description"
                Title=@LocalizationData.DeviceControl.TableFieldDescription 
                Type="string" Width="40%" />
            <RadzenGridColumn 
                TItem="TemplateResourceEntity" Property="ImageDataInfo"
                Title=@LocalizationData.DeviceControl.TableFieldImageDataInfo 
                Type="string" Width="25%" />
        </Columns>
    </RadzenGrid>

    <ActionsButtons ParentRazor=@this ButtonSettings=@ButtonSettings />
}
