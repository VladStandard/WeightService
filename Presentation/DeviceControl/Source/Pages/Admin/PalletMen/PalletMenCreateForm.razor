@using DeviceControl.Source.Widgets.Section
@using DeviceControl.Source.Widgets.Section.FormFields
@inherits DeviceControl.Source.Widgets.Section.SectionFormBase<Ws.Domain.Models.Entities.Ref.PalletManEntity>

<SectionForm
  FormModel="@DialogItem"
  Validator="@(new PalletMenCreateFormValidator())"
  OnCancelAction="@OnCancelAction"
  OnSubmitAction="@CreateItem"
>
  <FieldsContent>
    <SectionGuidField
      Label="UID 1C"
      @bind-Value="@DialogItem.Uid1C"
      For="@(() => DialogItem.Uid1C)"
    />

    <SectionTextField
      Label="@WsDataLocalizer["ColFirstName"]"
      @bind-Value="@DialogItem.Name"
      For="@(() => DialogItem.Name)"
    />

    <SectionTextField
      Label="@WsDataLocalizer["ColSurname"]"
      @bind-Value="@DialogItem.Surname"
      For="@(() => DialogItem.Surname)"
    />

    <SectionTextField
      Label="@WsDataLocalizer["ColPatronymic"]"
      @bind-Value="@DialogItem.Patronymic"
      For="@(() => DialogItem.Patronymic)"
    />

    <SectionTextField
      Label="@WsDataLocalizer["ColPassword"]"
      @bind-Value="@DialogItem.Password"
      For="@(() => DialogItem.Password)"
    />
  </FieldsContent>
</SectionForm>
