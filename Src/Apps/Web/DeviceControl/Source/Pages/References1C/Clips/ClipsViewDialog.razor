@using Ws.DeviceControl.Models.Shared
@implements IDialogContentComponent<PackageDto>

<SectionDialog>
  <Tab Id="@typeof(ClipsViewForm).ToString()" Title="@Localizer["SectionClips"]">
    <ClipsViewForm FormModel="Content" />
  </Tab>
</SectionDialog>

@code {
  [Inject] private IStringLocalizer<ApplicationResources> Localizer { get; set; } = default!;

  [Parameter] public PackageDto Content { get; set; } = default!;
}
