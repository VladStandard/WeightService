@using Ws.DeviceControl.Models.Dto.References1C.Brands

@implements IDialogContentComponent<BrandDto>

<SectionDialog>
  <Tab Id="@typeof(BrandsViewForm).ToString()" Title="@Localizer["SectionBrands"]">
    <BrandsViewForm FormModel="@Content" />
  </Tab>
</SectionDialog>

@code {
  [Inject] private IStringLocalizer<ApplicationResources> Localizer { get; set; } = default!;

  [Parameter] public BrandDto Content { get; set; } = default!;
}
