@using Ws.DeviceControl.Models.Dto.References1C.Plus
@using Ws.DeviceControl.Models.Dto.References1C.Plus.Queries

@implements IDialogContentComponent<PluDto>

<SectionDialog>
  <Tab Id="@typeof(PlusUpdateForm).ToString()" Title="@Localizer["SectionPLU"]">
    <PlusUpdateForm Model="Content" FormModel="@PluMapper.DtoToUpdateDto(Content)" />
  </Tab>
  @* <Tab Id="@typeof(PieceCharacteristicsDataGrid).ToString()" Title="@Localizer["SectionPluCharacteristics"]"> *@
  @*   @if (!DialogItem.IsWeight) *@
  @*   { *@
  @*     <PieceCharacteristicsDataGrid/> *@
  @*   } *@
  @*   else *@
  @*   { *@
  @*     <WeightCharacteristicsDataGrid /> *@
  @*   } *@
  @* </Tab> *@
</SectionDialog>

@code {
  [Inject] private IStringLocalizer<ApplicationResources> Localizer { get; set; } = default!;

  [Parameter] public PluDto Content { get; set; } = default!;
}
