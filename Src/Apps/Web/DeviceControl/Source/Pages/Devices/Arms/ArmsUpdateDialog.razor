@using Ws.DeviceControl.Models.Features.Devices.Arms
@using Ws.DeviceControl.Models.Features.Devices.Arms.Queries
@implements IDialogContentComponent<ArmDto>

<SectionDialog>
  <Tab Id="@typeof(ArmsUpdateForm).ToString()" Title="@Localizer["SectionLines"]">
    <ArmsUpdateForm Model="Content" FormModel="@ArmMapper.DtoToUpdateDto(Content)" />
  </Tab>
  @* <Tab Id="@typeof(ArmPluDataGrid).ToString()" Title="@Localizer["SectionLinePLU"]"> *@
  @*   <ArmPluDataGrid /> *@
  @* </Tab> *@
</SectionDialog>

@code {
  [Inject] private IStringLocalizer<ApplicationResources> Localizer { get; set; } = default!;

  [Parameter] public ArmDto Content { get; set; } = default!;
}
