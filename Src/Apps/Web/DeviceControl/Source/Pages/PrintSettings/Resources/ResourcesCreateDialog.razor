@implements IDialogContentComponent

<SectionDialog>
  <Tab Id="@typeof(ResourcesCreateForm).ToString()" Title="@Localizer["SectionResources"]">
    <ResourcesCreateForm FormModel="@new()" />
  </Tab>
</SectionDialog>

@code {
  [Inject] private IStringLocalizer<ApplicationResources> Localizer { get; set; } = default!;
}