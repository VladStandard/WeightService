@implements IDialogContentComponent

<SectionDialog>
  <Tab Id="@typeof(TemplatesCreateForm).ToString()" Title="@Localizer["SectionTemplates"]">
    <TemplatesCreateForm FormModel="@new()" />
  </Tab>
</SectionDialog>

@code {
  [Inject] private IStringLocalizer<ApplicationResources> Localizer { get; set; } = default!;
}
