@inherits SectionDialogBase<Ws.DeviceControl.Models.Dto.References.ProductionSites.Queries.ProductionSiteDto>

<SectionDialog DialogItem="DialogItem" TabsList="@TabsList">
  <Tab Id="@typeof(ProductionSitesCreateForm).ToString()">
    <ProductionSitesCreateForm />
  </Tab>
</SectionDialog>

@code {
  [Inject] private IStringLocalizer<ApplicationResources> Localizer { get; set; } = default!;

  protected override List<KeyValuePair<string, string>> InitializeTabList() =>
  [
    new(typeof(ProductionSitesCreateForm).ToString(), Localizer["SectionProductionSites"])
  ];
}
