@implements IDialogContentComponent

<SectionDialog>
  <Tab Id="@typeof(ProductionSitesCreateForm).ToString()" Title="@Localizer["SectionProductionSites"]">
    <ProductionSitesCreateForm FormModel="@(new())" />
  </Tab>
</SectionDialog>

@code {
  [Inject] private IStringLocalizer<ApplicationResources> Localizer { get; set; } = default!;
}
