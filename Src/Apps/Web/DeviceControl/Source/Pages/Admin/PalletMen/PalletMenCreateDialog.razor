@implements IDialogContentComponent

<SectionDialog>
  <Tab Id="@typeof(PalletMenCreateForm).ToString()" Title="@Localizer["SectionPalletMen"]">
    <PalletMenCreateForm FormModel="@new()" />
  </Tab>
</SectionDialog>

@code {
  [Inject] private IStringLocalizer<ApplicationResources> Localizer { get; set; } = default!;
}
