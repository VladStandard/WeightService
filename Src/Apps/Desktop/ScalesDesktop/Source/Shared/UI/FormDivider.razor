<h3 class="@Css.Class("flex py-2 font-semibold items-center w-full lg:col-span-2 before:mr-2 ",
             "before:content-[''] before:grow before:h-px before:bg-muted after:ml-2 after:content-[''] after:grow",
             "after:h-px after:bg-muted", Class)"
>
  @ChildContent
</h3>

@code {
  [Parameter] public RenderFragment? ChildContent { get; set; }
  [Parameter] public string Class { get; set; } = string.Empty;
}