@using DeviceControl.Source.Widgets.Section
@using Microsoft.Extensions.Localization
@inherits DeviceControl.Source.Widgets.Section.SectionDialogBase<Ws.Domain.Models.Entities.Ref.ProductionSiteEntity>


<SectionDialog DialogItem="DialogItem" TabsList="@TabsList">
  <Tab Id="typeof(ProductionSitesUpdateForm)">
    <ProductionSitesUpdateForm />
  </Tab>
</SectionDialog>

@code {
  [Inject] private IStringLocalizer<ApplicationResources> Localizer { get; set; } = default!;

  protected override List<KeyValuePair<Type, string>> InitializeTabList() =>
  [
    new(typeof(ProductionSitesUpdateForm),Localizer["SectionProductionSites"])
  ];
}