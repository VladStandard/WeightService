@using DeviceControl.Source.Widgets.Section
@using Microsoft.Extensions.Localization
@using Ws.Shared.Resources
@inherits DeviceControl.Source.Widgets.Section.SectionDialogBase<Ws.Domain.Models.Entities.DbFileSizeInfoEntity>


<SectionDialog DialogItem="DialogItem" TabsList="@TabsList">
  <Tab Id="typeof(FileTablesDataGrid)">
    <FileTablesDataGrid />
  </Tab>
</SectionDialog>

@code {
  [Inject] private IStringLocalizer<WsDataResources> WsDataLocalizer { get; set; } = default!;

  protected override List<KeyValuePair<Type, string>> InitializeTabList() =>
  [
    new(typeof(FileTablesDataGrid), WsDataLocalizer["ColTables"])
  ];
}