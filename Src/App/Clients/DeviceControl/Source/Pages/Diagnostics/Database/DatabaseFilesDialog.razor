@using DeviceControl.Source.Widgets.Section
@using Microsoft.Extensions.Localization
@using Ws.Shared.Resources
@inherits DeviceControl.Source.Widgets.Section.SectionDialogBase<Ws.Domain.Models.Entities.Diag.DbFileSizeInfo>


<SectionDialog DialogItem="DialogItem" TabsList="@TabsList">
  <Tab Id="@typeof(FileTablesDataGrid).ToString()">
    <FileTablesDataGrid />
  </Tab>
</SectionDialog>

@code {
  [Inject] private IStringLocalizer<WsDataResources> WsDataLocalizer { get; set; } = default!;

  protected override List<KeyValuePair<string, string>> InitializeTabList() =>
  [
    new(typeof(FileTablesDataGrid).ToString(), WsDataLocalizer["ColTables"])
  ];
}