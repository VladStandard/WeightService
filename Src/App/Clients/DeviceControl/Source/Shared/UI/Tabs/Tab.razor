@if (Control.ActiveTabId == Id)
{
  @ChildContent
}

@code {
  [Parameter, EditorRequired] public Type Id { get; set; } = default!;
  [CascadingParameter] private Tabs Control { get; set; } = default!;
  [Parameter] public RenderFragment? ChildContent { get; set; }
}